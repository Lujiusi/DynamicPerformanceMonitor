import java.util

/**
 * @author daiwei04@xinye.com
 * @date 2020/11/19 17:06
 * @desc
 */
object StrTest {

  def main(args: Array[String]): Unit = {
    //    val s = "s=2,3=2 d=e,dr=e"
    //    val result = new util.HashMap[String, String]()
    //    List(s.substring(0, s.lastIndexOf(' ')), s.substring(s.lastIndexOf(' ') + 1)).flatMap(_.split(",")).map(_.split("="))
    //      .foreach(tuple => {
    //        result.put(tuple(0), tuple(1))
    //      })

    //    print(result)

    //    val per = new Person("dfa", 1)

    val a = if (true) 1 else 2


    print(a)
    //    print(per.getAge)

  }

}
